# Environment Variables Configuration
# Copy this file to .env and set your values
# The .env file is gitignored and will not be committed

# ============================================
# Build-time Variables (Vite - Frontend)
# ============================================
# These variables are embedded into the frontend bundle during build
# They must be set when running: npm run build or ./start-background.sh --prod

# Enable browser restriction in production
# Set to 'true' to restrict access to specific browsers (see browserConfig.js)
# Only applies in production mode
VITE_ENABLE_BROWSER_RESTRICTION=false

# API URL for frontend (optional, defaults to http://localhost:33001)
# VITE_API_URL=http://localhost:33001

# ============================================
# Runtime Variables (Backend/Node.js)
# ============================================
# These variables are used at runtime by the backend server
# They can be set in ecosystem.config.js (for PM2) or as environment variables

# Node environment (development or production)
# NODE_ENV=production

# Server host (0.0.0.0 for network access, 127.0.0.1 for localhost only)
# HOST=0.0.0.0

# Server port (default: 33001)
# PORT=33001

# Frontend URL for CORS validation (optional)
# FRONTEND_URL=http://localhost:33000

# Multiple frontend URLs (comma-separated)
# FRONTEND_URLS=http://localhost:33000,http://192.168.1.100:33000
